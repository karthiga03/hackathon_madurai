@using DoctorAppointmentScheduler.Models
@model IEnumerable<DoctorAppointmentScheduler.Models.DoctorDetail>

@{
    ViewBag.Title = "DoctorAvailability";
}

<h2>DoctorAvailability</h2>

 
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DoctorID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DoctorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Specaility)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DoctorID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DoctorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Specaility)
        </td>
       

        <td>
            @Html.ActionLink("Book", "BookAppointment", new {  id=item.DoctorName }) |
          
        </td>
    </tr>
}

</table>
@Html.ActionLink("Back to Login Page", "Login","Login")